// & Tipos de Dados

interface A {}

// * String
const text: string = "10";

// * Boolean
const isNew: boolean = false;

// * Number
const age = 10;

// * Array
const cars: string[] = ["BMW", "Volks"];
const _cars: Array<string> = ["BMW", "Volks"];

// * Tuple
const personInformation: [name: string, age: number] = ["John", 10];

// * Objetos
const userInformation = {
    name: "John",
    age: 10,
};

// ! Inferência de Tipos
